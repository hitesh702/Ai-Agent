# CallAI Frontend (Vite)

Simple React + Vite marketing frontend.

## Run

```bash
cd frontend
npm install
npm run dev
```

Open the local URL shown in the terminal (usually http://localhost:5173).

This folder does **not** replace the existing Next.js app until you approve that.
